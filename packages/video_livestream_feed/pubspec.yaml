name: video_livestream_feed
description: "A showcase app for a TikTok-style livestream feed with call switching."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  stream_video_flutter: ^1.4.0
  permission_handler: ^12.0.1
  video_player: ^2.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
